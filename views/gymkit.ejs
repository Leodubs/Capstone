<!DOCTYPE html>
<html lang="fr" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>Gym Kit</title>
  <link rel="stylesheet" href="/styles/gymkit.css">
  <link href="https://fonts.googleapis.com/css?family=Arvo" rel="stylesheet">
</head>

<body>

  <h1 id="title">🤸 Routine 🪑</h1>

  <!-- player -->
  <figure>
    <figcaption></figcaption>
    <audio id="welcome" controls>
      <!-- <a href="..."> Download audio </a> -->
    </audio>
  </figure>

  <!-- kit audio indépendant-->
  <div class="set">
    <!-- displayed -->
    <button id="d" class="selection">dos & bassin</button>
    <button id="f" class="selection">fessiers</button>
    <button id="g" class="selection">gainage</button>
    <button id="q" class="selection">quadriceps</button>
    <button id="a" class="selection">adducteurs</button>
    <button id="i" class="selection">ischio</button>
    <!-- not displayed -->
    <button id="all" class="selection">all</button>
  </div>


  <footer>
    Made with ❤️ in Barsac.
  </footer>

<script>

// FONCTION "makeSound"
function makeSound(btnClicked) {
    switch (btnClicked) {
        case "d":
            sound.src = (trackList[1]);
            break;
        case "f":
            sound.src = (trackList[2]);
            break;
        case "g":
            sound.src = (trackList[3]);
            break;
        case "q":
            sound.src = (trackList[4]);
            break;
        //   case "a":

        //     break;
    
        //   case "i":

        //     break;
        default: 
            sound.src = (errorTrack);
            break;
    }
    audioPlayer.pause();
    sound.play();        
    console.log("🎵 CURRENTLY PLAYING " + sound.src);
}
</script>
</body>

</html>
